# Pour cloner un repository
git clone <URL_du_repository>

# Les 3 commandes de push
git add .
git commit -m "Message du commit"
git push

# Pour effectuer un pull depuis le repository distant
git pull origin master
Ajout de la commande pull
